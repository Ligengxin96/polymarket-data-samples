# 数据使用说明

(English version: DATA_GUIDE.md)

## 目录结构

样例包解压后的目录结构与付费归档完全一致，因此针对样例写的代码，换成正式交付的
数据集可以原样运行：

```
polymarket-data-samples/
  data/polymarket/daily/markets/BTC-5m/BTC-5m-markets-<日期>.jsonl.gz
  data/polymarket/daily/book/BTC-5m/BTC-5m-book-<日期>.jsonl.gz
  data/polymarket/daily/best_bid_ask/BTC-5m/BTC-5m-best_bid_ask-<日期>.jsonl.gz
  data/polymarket/daily/price_change/BTC-5m/BTC-5m-price_change-<日期>.jsonl.gz
  data/polymarket/daily/last_trade_price/BTC-5m/BTC-5m-last_trade_price-<日期>.jsonl.gz
  data/chainlink/daily/prices/BTCUSD/BTCUSD-prices-<日期>.csv.gz
  data/chainlink-twap-30s/daily/prices/BTCUSD/BTCUSD-twap30s-prices-<日期>.csv.gz
  data/chainlink-twap-60s/daily/prices/BTCUSD/BTCUSD-twap60s-prices-<日期>.csv.gz
```

目录层级本身带有含义，工具会按它解析：市场数据是
`data/<场馆>/daily/<数据类型>/<币种>-<周期>/`，价格线是
`data/<结算流>/daily/prices/<符号>/`。完整购买的数据集结构相同，只是币种、周期与
天数更多。

## <符号>-prices-<日期>.csv.gz —— Chainlink 结算价格

| 字段 | 含义 |
|---|---|
| feed_ts_ms | 价格事件时间（毫秒，整秒对齐）|
| value | 价格（浮点，便利用）|
| full_accuracy_value | 精确价格：1e18 倍整数字符串，除以 1e18 得真实价格 |
| server_ts_ms | relay 服务端发送时间 |
| recv_ms | 采集端接收时间 |

注：同一秒出现两行不同值 = feed 同秒修正，recv_ms 晚者为准。

用 Excel 打开的注意事项：full_accuracy_value 超过 Excel 的 15 位数字精度上限，直接双击打开会显示成科学计数法且丢精度。日常查看请用 value 列；确需全精度时用「数据 -> 自文本/CSV 导入」，并把 full_accuracy_value 列类型设为「文本」。

## <系列>-markets-<日期>.jsonl.gz —— 每局市场的元数据与结算结果

| 字段 | 含义 |
|---|---|
| slug | 市场 id，后缀为 slot 起点（unix 秒）|
| start_sec / end_sec | 局的起止时间（unix 秒）|
| interval_sec | 300 = 5 分钟局，900 = 15 分钟局 |
| token_ids | CLOB token id，[Up, Down] 顺序 |
| resolved | 是否已结算 |
| outcome_prices | ["1","0"] Up 赢，["0","1"] Down 赢，["0.5","0.5"] 平分 |
| strike_value | 起点价（priceToBeat），1e18 倍整数字符串；起点秒无 tick 时为 null |
| raw | Gamma API 完整市场对象 |

注：结算规则 = 取 end_sec 当时或之前最近一条 feed tick（收盘生效值；feed 约 1Hz，不一定正好落在 end_sec），**大于或等于** strike_value 则 Up 赢——官方市场规则原文是 "greater than or equal to"，即持平也判 Up。少数局落在已公示的 feed 断流窗（end_sec 附近无 tick）或 strike_value 为 null——见 coverage 报告，这些无法仅凭 feed 重算。跨午夜的局在两天文件里都出现，按 slug 去重。

结算源切换：**2026-08-07 00:00 UTC** 起的市场（`raw.cryptoMarketConfig.twapEnabled = true`）改用 Chainlink **TWAP 流**结算——收盘时刻 TWAP 流的值 ≥ 开盘时刻的值则 Up 赢。

**用哪条流结算，写在市场自己身上**，即 `raw.cryptoMarketConfig.twapLookbackSeconds`——请逐个市场读这个字段，不要按日期推断，因为上游已经改过一次：

- 2026-08-07 起：5 分钟市场用 30 秒回看流，15 分钟市场用 60 秒流
- 2026-08-13 起上游开始把 5 分钟市场迁到 60 秒流，08-14 迁完；**2026-08-15 起，5 分钟与 15 分钟市场全部都用 60 秒流结算**

`twap30s` 流仍在采集、仍随每一天的数据集出货，但它已不再决定任何市场的输赢。完整数据集以 `twap30s`/`twap60s` 价格文件提供这两条流（字段与 `prices` 相同，覆盖自 2026-08-08 起）——切换后的市场请用市场自己 config 指明的那条流重算，不要用瞬时 `prices` 文件。

## <系列>-book-<日期>.jsonl.gz —— 订单簿全深度快照

| 字段 | 含义 |
|---|---|
| slug | 所属市场 |
| asset_id | 快照所属的 token（Up 或 Down）|
| event_ts_ms | CLOB 帧时间 |
| recv_ms | 采集端接收时间 |
| payload.bids[] / payload.asks[] | 全部价位档，{price, size} 字符串 |

注：t 时刻盘口 = 该 token recv_ms <= t 的最近一条快照。

## <系列>-best_bid_ask-<日期>.jsonl.gz —— 盘口顶部（未限流）

| 字段 | 含义 |
|---|---|
| slug | 市场 |
| asset_id | 该报价所属 token（每个市场两条腿）|
| event_type | 本文件中恒为 `best_bid_ask` |
| event_ts_ms | **场方事件时刻，epoch 毫秒——请用这个** |
| recv_ms | 采集端接收时刻 |
| payload.best_bid / best_ask | 最优买价 / 最优卖价 |
| payload.spread | 上游给出的价差 |
| payload.timestamp | 场方事件时刻，**字符串形式** |
| payload.market / asset_id / event_type | 上游原样字段 |

**`payload` 内的每个值都是字符串**，时间戳也是。我们已把场方事件时刻规范化到顶层的
数字字段 `event_ts_ms`，请直接读它，不必去解析 `payload.timestamp`——541,310 行实测
两者逐行相等。

只要某条腿的盘口顶部发生变动，场方就推一条。**只有价格、没有挂单量**；要深度请用
`book` 全量快照与 `price_change` 增量。同一市场的两条腿通常成对推送（2026-09-04
实测：成对 646,992 组，单腿 2,611 组）。

**空的一侧是用价格编码的**，不是 null、也不是字段缺失；又因为同一市场两个结果的
价格恒和为 1，它在两条腿上的写法不同：`best_bid = "0"` 表示**无人出价**，
`best_ask = "1"` 表示**无人在上限以下挂卖**。二者是同一件事在互补 token 上的两种写法，
计数可以佐证——样例日 `best_bid = "0"` 有 11,694 帧，`best_ask = "1"` 恰好也是 11,694 帧
（合计 23,388 行、占 1.78%，没有任何一行两侧同时为空）。

因此判断要用**闭区间 [0, 1]**，而不是活跃报价的 [0.001, 0.999]。这里的 `"1"` 不是
坏值，把它当坏值清洗掉，扔掉的恰恰是盘口状态最明确的那批行。

**这个文件为什么存在。**`price_change` 的每条变更本来就带 best_bid/best_ask，但它是
限流的，只靠增量重建的盘口顶部必然漏掉一部分变动。把「一次变动」定义为某 token 的
(最优买价, 最优卖价) 发生变化、两个文件用同一口径计数：样例日本文件记录了
**487,856 次**，而增量流只能还原 **313,116 次，即 64.2%**。BTC 还是最好的情况
（增量对 BTC 每 20ms 保留一条、ETH 100ms、其余 500ms），其它币种能留下的远少于此。

数据起点：2026-09-02（当天 00:42:18 UTC 开始采集，故缺开头 42 分钟），首个完整
UTC 日为 2026-09-03。更早的日期没有等价数据——那时盘口顶部只存在于限流的
`price_change` 里。

## <系列>-price_change-<日期>.jsonl.gz —— 盘口增量（best bid/ask）

| 字段 | 含义 |
|---|---|
| slug | 所属市场 |
| event_ts_ms | CLOB 帧时间 |
| recv_ms | 采集端接收时间 |
| payload.price_changes[] | 每个变动档一条：{side, price, size, best_bid, best_ask, asset_id} |

注：亚秒级盘口增量。按 event_ts_ms 顺序叠加在最近一条 book 快照上，即可还原两次快照之间的 best bid/ask。成交永不限流。

限流（已公示）：帧按「对每个市场，每 N 毫秒最多 1 条」限流（窗口内保留第一条），且 N 随**日期与币种**变化：

- 2026-06-06 起：全部 500ms
- 2026-08-25 起：BTC 20ms、ETH 100ms，其余全部 500ms

因此 2026-08-25 起的 BTC 日，增量条数约为此前的 25 倍。若跨越该日期重建盘口，分辨率会随之变化。

## <系列>-last_trade_price-<日期>.jsonl.gz —— 全部成交

| 字段 | 含义 |
|---|---|
| payload.price / size | 成交价 / 量 |
| payload.side | BUY = 吃单方为买 |
| payload.asset_id | 成交的 token |
| payload.fee_rate_bps | 手续费率（基点）|
| payload.timestamp | 成交时间（毫秒）|
| recv_ms | 采集端接收时间 |

## manifest.json —— 文件清单，每个文件的行数与 sha256 校验值
